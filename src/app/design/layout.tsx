// This is a new layout file for the design studio.
// It provides a clean, full-screen environment without the main app's sidebar or footer.

export default function DesignStudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
